<Window x:Class="FAI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FAI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Automatic toggle and manual control area -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Left">


                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="SN:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <TextBox x:Name="txtSN" Width="160"/>
                    <Button x:Name="btnCheckSN" Content="Find SN" Click="btnCheckSN_Click" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
            <TextBlock x:Name="txtManualResult" FontWeight="Bold" Margin="10,10,10,10" Grid.Row="4" >
            </TextBlock>

            <Button Content="Test" HorizontalAlignment="Left" Margin="268,0,0,0" VerticalAlignment="Center" Click="Button_Click"/>




        </Grid>

    </Grid>
</Window>
