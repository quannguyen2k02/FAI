<Window x:Class="FAI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FAI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtNextRun" Grid.Row="0" FontSize="14" FontWeight="Bold" Margin="5"/>
            <TextBlock Grid.Row="1" Text="Lịch sử hoạt động:" Margin="5"/>
            <ListBox x:Name="lstLog" Grid.Row="2" Margin="5"/>
        </Grid>

    </Grid>
</Window>
